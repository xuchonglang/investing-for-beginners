# RPC 节点为什么会影响钱包？

RPC 节点是钱包和应用读取区块链、查询余额、估算 Gas 与广播交易的通信入口。

完整词条：[RPC 节点是什么？](https://xiaoyinsi.com/wiki/crypto/rpc-node)

钱包显示余额异常或交易无法广播时，可能只是当前 RPC 延迟或故障，更换可信节点有时即可解决。

RPC 提供者可以观察请求，并可能返回延迟或错误数据。私钥若保存在本地，RPC 通常不能替你签名，但虚假信息仍可能误导操作。

重要交易应核对区块浏览器和多个数据源，不要把前端或单一 RPC 的显示当成最终链上事实。
